matrix:
  include:
     - language: go
       os: linux
       deploy:
         provider: releases
         api_key: ${GITHUB_OAUTH_TOKEN}
         file: dnsleaktest.tar.gz
         skip_cleanup: true
         on:
           tags: false
           repo: macvk/dnsleaktest


